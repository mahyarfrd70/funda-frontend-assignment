# Copy to `.env` and fill in. The .env file is git-ignored.

# Funda Partner API key (temporary key supplied with the assignment).
NUXT_FUNDA_API_KEY=<NUXT_FUNDA_API_KEY>

# Base URL for the Aanbod feed. Override only if the endpoint changes.
NUXT_FUNDA_API_BASE=https://partnerapi.funda.nl/feeds/Aanbod.svc/json
